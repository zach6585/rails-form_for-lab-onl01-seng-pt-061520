<!DOCTYPE html>

<html>
    <%= form_for(@school_class) do |f| %>
        <label>School Class Form</label>
        <%= f.label :title %>
        <%= f.text_field :title %> 
        <%= f.number_field :room_number %>
        <%= f.submit %>
    <% end %>
</html>